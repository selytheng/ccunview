
export interface Course {
    id: number;
    name: string;
    description: string;
    image: string; 
  }

export interface Partner {
    id: number | string;
    name: string;
    description: string;
}
  
export interface Major {
  id: number;
  name: string;
  description: string;
}
